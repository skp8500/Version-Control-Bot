# Base path for the frontend (leave as "/" for Vercel root deployments)
BASE_PATH=/

# URL of your Render API server — set this in Vercel's environment variables
# Leave empty when running locally (Vite proxy handles it)
VITE_API_URL=https://mygit-api.onrender.com
